services:
  sokyo:
    build: .
    container_name: sokyo
    restart: unless-stopped
    ports:
      - "127.0.0.1:${SOKYO_PORT:-3010}:3000"
    environment:
      - ADMIN_PIN=${ADMIN_PIN:-}
    volumes:
      - ./data:/app/data
